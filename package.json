{
  "name": "notes",
  "version": "1.0.0",
  "description": "Full stack notes app",
  "homepage": "https://github.com/mbportz/notes-app#readme",
  "bugs": {
    "url": "https://github.com/mbportz/notes-app/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mbportz/notes-app.git"
  },
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "lint-staged": {
    "NotesApp/**/*.{js,jsx,ts,tsx}": [
      "npx --prefix NotesApp eslint --fix --cache",
      "prettier --write"
    ],
    "*.{js,jsx,ts,tsx}": [
      "prettier --write"
    ],
    "*.{json,md,yml,yaml}": [
      "prettier --write"
    ]
  },
  "scripts": {
    "typecheck": "echo 'define me at root (turbo/nx/workspaces foreach)'",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "prettier": "^3.6.2"
  },
  "dependencies": {
    "zod": "4.1.12"
  }
}
